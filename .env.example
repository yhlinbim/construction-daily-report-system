# Copy to .env for `docker compose up`. .env is gitignored.
# SQL Server requires a strong SA password: 8+ chars with three of
# uppercase, lowercase, digits, symbols.
MSSQL_SA_PASSWORD=Change_this_local_password_1
